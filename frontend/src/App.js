import {useEffect, useMemo, useState} from 'react';
import axios from 'axios';
import {ShieldCheck, LockKeyhole, Plus, Search, Eye, EyeOff, Copy, Download, Trash2, LogOut, KeyRound, ArrowUpRight, X, Check, RefreshCw, Pencil, Wand2, Upload, Share2, Activity, ShieldAlert, Gauge, Timer} from 'lucide-react';
import './App.css';
import './brand.css';
import {Toaster, toast} from 'sonner';

const API=`${process.env.REACT_APP_BACKEND_URL}/api`;
const client=axios.create({baseURL:API});
const authHeader=()=>({headers:{Authorization:`Bearer ${localStorage.getItem('vault_token')}`} });

function Auth({onLogin}){
  const [mode,setMode]=useState('login'); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [busy,setBusy]=useState(false);
  const submit=async(e)=>{e.preventDefault(); setBusy(true); try{const r=await client.post(`/auth/${mode==='login'?'login':'register'}`,{email,password}); localStorage.setItem('vault_token',r.data.token); onLogin(r.data.user)}catch(e){toast.error(e.response?.data?.detail||'Could not sign in')}finally{setBusy(false)}};
  return <main className="auth-shell"><section className="auth-art"><div className="brand"><img src="/toppass5-logo-sm.jpeg" alt="TopPass5" className="brand-logo-mark"/></div><div className="art-copy"><p className="eyebrow">PRIVATE BY DESIGN</p><h1>Your secrets.<br/><em>Only yours.</em></h1><p>One quiet place for the values that matter. Encrypted before they leave your device.</p></div><div className="security-stamp"><LockKeyhole size={17}/><span>Zero-knowledge architecture<br/><b>Built for calm control</b></span></div></section><section className="auth-panel"><div className="auth-card"><div className="mobile-brand brand"><img src="/toppass5-logo-sm.jpeg" alt="TopPass5" className="brand-logo-mark"/></div><p className="eyebrow">SECURE ACCESS</p><h2>{mode==='login'?'Welcome back':'Create your vault'}</h2><p className="muted">{mode==='login'?'Your private command center is waiting.':'Start protecting what matters in under a minute.'}</p><form onSubmit={submit} data-testid="auth-form"><label>Email<input data-testid="auth-email-input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></label><label>Password<input data-testid="auth-password-input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength="8" placeholder="At least 8 characters"/></label><button className="primary wide" data-testid="auth-submit-button" disabled={busy}>{busy?'Securing…':mode==='login'?'Unlock vault':'Create vault'} <ArrowUpRight size={17}/></button></form><div className="or"><span>or continue with</span></div><a className="google-btn" data-testid="google-login-button" href={`${API}/auth/google?frontend_origin=${encodeURIComponent(window.location.origin)}`}><span className="google-g">G</span> Continue with Google</a><button className="link-btn" data-testid="auth-mode-toggle" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?"I don't have an account":"I already have an account"}</button><button className="recovery-link" data-testid="recovery-button" onClick={()=>{const e=prompt('Enter your account email'); if(e) client.post('/auth/recovery',{email:e}).then(r=>toast.success(r.data.recovery_code?`Recovery code: ${r.data.recovery_code}`:r.data.message))}}>Need recovery help?</button></div></section></main>
}

function Vault({user,onLogout}){
  const [items,setItems]=useState([]); const [query,setQuery]=useState(''); const [showForm,setShowForm]=useState(false); const [editing,setEditing]=useState(null); const [visible,setVisible]=useState({}); const [values,setValues]=useState({}); const [form,setForm]=useState({name:'',value:'',category:'Secret',totp_secret:''}); const [showGen,setShowGen]=useState(false); const [genOpts,setGenOpts]=useState({length:16,upper:true,lower:true,nums:true,syms:false}); const [genPwd,setGenPwd]=useState(''); const [cat,setCat]=useState('All'); const [shareModal,setShareModal]=useState(null); const [showAudit,setShowAudit]=useState(false); const [auditLogs,setAuditLogs]=useState([]); const [secReport,setSecReport]=useState(null); const [showSec,setShowSec]=useState(false); const [categories,setCategories]=useState(['Login','API key','Secret','Secure note','Wi-Fi','Bank']);
  const load=()=>{client.get('/items',authHeader()).then(r=>setItems(r.data)).catch(()=>onLogout()); client.get('/preferences',authHeader()).then(r=>{if(r.data.categories)setCategories(r.data.categories)}).catch(()=>{})}; useEffect(()=>{load()},[]);
  const filtered=useMemo(()=>items.filter(i=>i.name.toLowerCase().includes(query.toLowerCase())&&(cat==='All'||i.category===cat)),[items,query,cat]);
  const save=async(e)=>{e.preventDefault(); try{if(editing) await client.put(`/items/${editing.id}`,form,authHeader()); else await client.post('/items',form,authHeader()); if(form.category&&!categories.includes(form.category))saveCategory(form.category); toast.success(editing?'Item updated':'Item encrypted and saved'); setShowForm(false);setEditing(null);setForm({name:'',value:'',category:'Secret',totp_secret:''});load()}catch(e){toast.error(e.response?.data?.detail||'Save failed')}};
  const reveal=async(id)=>{if(values[id]){setVisible({...visible,[id]:!visible[id]});return values[id]} const r=await client.get(`/items/${id}/value`,authHeader()); setValues({...values,[id]:r.data.value});setVisible({...visible,[id]:true});return r.data.value};
  const copy=async(id)=>{const value=values[id]||await reveal(id);try{await navigator.clipboard.writeText(value)}catch{const area=document.createElement('textarea');area.value=value;document.body.appendChild(area);area.select();document.execCommand('copy');area.remove()}toast.success('Copied to clipboard');};
  const remove=async(id)=>{if(window.confirm('Delete this item permanently?')){await client.delete(`/items/${id}`,authHeader());toast.success('Item deleted');load()}};
  const startEdit=async(item)=>{let v=values[item.id];if(!v){const r=await client.get(`/items/${item.id}/value`,authHeader());v=r.data.value;setValues(p=>({...p,[item.id]:v}))}setEditing(item);setForm({name:item.name,value:v,category:item.category,totp_secret:''});setShowForm(true)};
  const mkPwd=(o)=>{let c='';if(o.upper)c+='ABCDEFGHIJKLMNOPQRSTUVWXYZ';if(o.lower)c+='abcdefghijklmnopqrstuvwxyz';if(o.nums)c+='0123456789';if(o.syms)c+='!@#$%^&*()-_=+[]{}|;:,.<>?';if(!c)c='abcdefghijklmnopqrstuvwxyz0123456789';return Array.from({length:o.length},()=>c[Math.floor(Math.random()*c.length)]).join('')};
  const b32d=s=>{const a='ABCDEFGHIJKLMNOPQRSTUVWXYZ234567';let bits=0,val=0,out=[];for(const c of s.toUpperCase().replace(/[^A-Z2-7]/g,'')){val=(val<<5)|a.indexOf(c);bits+=5;if(bits>=8){out.push((val>>>(bits-8))&0xFF);bits-=8}}return new Uint8Array(out)};
  const getTOTP=async secret=>{const key=b32d(secret);const T=Math.floor(Date.now()/30000);const msg=new Uint8Array(8);new DataView(msg.buffer).setUint32(4,T);const k=await crypto.subtle.importKey('raw',key,{name:'HMAC',hash:'SHA-1'},false,['sign']);const sig=new Uint8Array(await crypto.subtle.sign('HMAC',k,msg));const o=sig[19]&0xf;return(((sig[o]&0x7f)<<24|(sig[o+1]&0xff)<<16|(sig[o+2]&0xff)<<8|(sig[o+3]&0xff))%1000000).toString().padStart(6,'0')};
  const revealTOTP=async(item)=>{try{const r=await client.get(`/items/${item.id}/totp`,authHeader());const code=await getTOTP(r.data.secret);navigator.clipboard.writeText(code).catch(()=>{});toast.success(`OTP: ${code.slice(0,3)} ${code.slice(3)} — copied!`,{duration:6000})}catch{toast.error('No TOTP configured')}};
  const sha1hex=async str=>{const buf=await crypto.subtle.digest('SHA-1',new TextEncoder().encode(str));return Array.from(new Uint8Array(buf)).map(b=>b.toString(16).padStart(2,'0')).join('').toUpperCase()};
  const checkBreach=async(item)=>{let val=values[item.id];if(!val){const r=await client.get(`/items/${item.id}/value`,authHeader());val=r.data.value;setValues(p=>({...p,[item.id]:val}))}try{const h=await sha1hex(val);const resp=await fetch(`https://api.pwnedpasswords.com/range/${h.slice(0,5)}`);const text=await resp.text();const found=text.split('\n').find(l=>l.startsWith(h.slice(5)));if(found){toast.error(`Leaked in ${parseInt(found.split(':')[1]).toLocaleString()} breaches!`,{duration:6000})}else{toast.success('Not found in any known breach')}}catch{toast.error('Breach check failed')}};
  const loadSecReport=async()=>{try{const r=await client.get('/security/report',authHeader());setSecReport(r.data);setShowSec(true)}catch{toast.error('Could not load report')}};
  const saveCategory=async newCat=>{if(newCat&&!categories.includes(newCat)){const updated=[...categories,newCat];setCategories(updated);client.put('/preferences',{categories:updated},authHeader()).catch(()=>{})}};
  const importVault=async(e)=>{const file=e.target.files[0];if(!file)return;try{const raw=JSON.parse(await file.text());const arr=Array.isArray(raw)?raw:[raw];const valid=arr.filter(x=>x.name&&x.value).map(x=>({name:x.name,value:x.value,category:x.category||'Secret'}));if(!valid.length){toast.error('No valid items found in file');e.target.value='';return}await client.post('/items/import',{items:valid},authHeader());toast.success(`${valid.length} item(s) imported`);load()}catch{toast.error('Import failed — check file format')}finally{e.target.value=''}};
  const shareItem=async(item)=>{try{const r=await client.post(`/items/${item.id}/share`,{hours:24},authHeader());const link=`${window.location.origin}/?share=${r.data.token}`;setShareModal({item,link,hours:r.data.hours});toast.success('Share link ready!')}catch(e){toast.error(e.response?.data?.detail||'Share failed')}};
  const loadAudit=async()=>{try{const r=await client.get('/audit',authHeader());setAuditLogs(r.data);setShowAudit(true)}catch{toast.error('Could not load activity')}};
  const exportItem=(item)=>{const data=JSON.stringify({name:item.name,value:values[item.id]||'[reveal before export]',category:item.category},null,2);const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([data],{type:'application/json'}));a.download=`${item.name.replace(/\s+/g,'-')}.json`;a.click()};
  const exportAll=async()=>{if(!window.confirm('This will download ALL your secrets in plain text. Continue?'))return;const full=await Promise.all(items.map(async i=>{const r=await client.get(`/items/${i.id}/value`,authHeader());return {...i,value:r.data.value}}));const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(full,null,2)],{type:'application/json'}));a.download='toppass5-backup.json';a.click();toast.success('Vault backup downloaded')};
  return <div className="app-shell"><aside className="sidebar"><div className="brand"><img src="/toppass5-logo-sm.jpeg" alt="TopPass5" className="brand-logo-mark"/></div><div className="side-label">YOUR SPACE</div><div className="nav-active" data-testid="vault-nav"><KeyRound size={17}/>Vault <span data-testid="item-count">{items.length}</span></div><div className="nav-item" data-testid="activity-nav" onClick={loadAudit}><Activity size={17}/>Activity</div><div className="nav-item" data-testid="security-report-nav" onClick={loadSecReport}><Gauge size={17}/>Security Report</div><div className="side-label lower">SECURITY</div><div className="side-note"><span className="status-dot"/>All systems protected</div><div className="side-bottom"><div className="user-pill"><div className="avatar">{user.name?.[0]?.toUpperCase()}</div><div><b data-testid="user-email">{user.email}</b><small>Personal vault</small></div></div><button className="icon-btn" data-testid="logout-button" onClick={onLogout} title="Sign out"><LogOut size={17}/></button></div></aside><main className="vault-main"><header className="topbar"><div><p className="eyebrow">PERSONAL VAULT / TODAY</p><h1>Good to see you, {user.name?.split(' ')[0]}</h1></div><div className="top-actions"><label className="secondary top-btn" data-testid="import-button" title="Import from backup JSON"><Upload size={16}/> Import<input type="file" accept=".json" style={{display:'none'}} onChange={importVault}/></label><button className="secondary top-btn" data-testid="export-all-button" onClick={exportAll}><Download size={16}/> Export</button><button className="secondary top-btn" data-testid="generator-button" onClick={()=>{setGenPwd(mkPwd(genOpts));setShowGen(true)}}><Wand2 size={16}/> Generator</button><button className="primary" data-testid="add-item-button" onClick={()=>{setEditing(null);setForm({name:'',value:'',category:'Secret'});setShowForm(true)}}><Plus size={17}/> Add value</button></div></header><section className="metrics"><div><span>Protected values</span><strong data-testid="protected-count">{items.length.toString().padStart(2,'0')}</strong></div><div><span>Security health</span><strong className="green">Excellent <Check size={17}/></strong></div><div><span>Last activity</span><strong>Just now</strong></div></section><section className="vault-section"><div className="section-head"><div><p className="eyebrow">YOUR COLLECTION</p><h2>Encrypted values</h2></div><label className="search"><Search size={17}/><input data-testid="vault-search-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search your vault…"/></label></div><div className="cat-filter">{['All',...new Set(categories)].map(c=><button key={c} className={`cat-chip${cat===c?' active':''}`} data-testid={`cat-filter-${c.replace(/\s+/g,'-')}`} onClick={()=>setCat(c)}>{c}</button>)}</div>{filtered.length===0?<div className="empty-state" data-testid="empty-vault-state"><div className="empty-icon"><LockKeyhole/></div><h3>{query?'Nothing found':'Your vault is quiet'}</h3><p>{query?'Try another name.':'Add your first value and keep it protected.'}</p><button className="primary" data-testid="empty-add-button" onClick={()=>setShowForm(true)}><Plus size={17}/> Add your first value</button></div>:<div className="item-list">{filtered.map(item=><article className="item-row" key={item.id} data-testid={`vault-item-${item.id}`}><div className="item-icon"><LockKeyhole size={18}/></div><div className="item-info"><b data-testid={`item-name-${item.id}`}>{item.name}</b><span>{item.category} · Updated {new Date(item.updated_at).toLocaleDateString()}</span></div><div className="secret-preview" data-testid={`item-value-${item.id}`}>{visible[item.id]?values[item.id]:'••••••••••••'}</div><div className="item-actions">{item.has_totp&&<button className="icon-btn totp-btn" data-testid={`totp-item-${item.id}`} onClick={()=>revealTOTP(item)} title="Get OTP code"><Timer size={16}/></button>}<button className="icon-btn" data-testid={`reveal-item-${item.id}`} onClick={()=>reveal(item.id)} title="Reveal"><Eye size={17}/></button><button className="icon-btn" data-testid={`copy-item-${item.id}`} onClick={()=>copy(item.id)} title="Copy"><Copy size={17}/></button><button className="icon-btn" data-testid={`breach-item-${item.id}`} onClick={()=>checkBreach(item)} title="Check breach"><ShieldAlert size={16}/></button><button className="icon-btn" data-testid={`download-item-${item.id}`} onClick={()=>exportItem(item)} title="Download"><Download size={17}/></button><button className="icon-btn" data-testid={`share-item-${item.id}`} onClick={()=>shareItem(item)} title="Share"><Share2 size={16}/></button><button className="icon-btn" data-testid={`edit-item-${item.id}`} onClick={()=>startEdit(item)} title="Edit"><Pencil size={16}/></button><button className="icon-btn danger" data-testid={`delete-item-${item.id}`} onClick={()=>remove(item.id)} title="Delete"><Trash2 size={17}/></button></div></article>)}</div>}</section></main>{showForm&&<div className="modal-backdrop"><form className="modal" onSubmit={save} data-testid="item-form"><button type="button" className="modal-close icon-btn" data-testid="close-item-modal" onClick={()=>{setShowForm(false);setEditing(null);setForm({name:'',value:'',category:'Secret',totp_secret:''})}}><X/></button><p className="eyebrow">{editing?'EDIT VALUE':'NEW VALUE'}</p><h2>{editing?'Update protected value':'Add to your vault'}</h2><label>Name<input data-testid="item-name-input" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required placeholder="e.g. Wi-Fi password"/></label><label>Value<div className="gen-row"><textarea data-testid="item-value-input" value={form.value} onChange={e=>setForm({...form,value:e.target.value})} required placeholder="Your secret value" rows="4"/><button type="button" className="gen-inline" data-testid="generate-inline-button" onClick={()=>setForm({...form,value:mkPwd(genOpts)})} title="Generate password"><Wand2 size={14}/> Generate</button></div></label><label>Category<input list="cat-opts" data-testid="item-category-input" value={form.category} onChange={e=>setForm({...form,category:e.target.value})} placeholder="e.g. Login, API key…"/><datalist id="cat-opts">{categories.map(c=><option key={c} value={c}/>)}</datalist></label><label>Authenticator Secret (TOTP) — optional<input data-testid="item-totp-input" type="text" value={form.totp_secret||''} onChange={e=>setForm({...form,totp_secret:e.target.value})} placeholder="Base32 secret e.g. JBSWY3DPEHPK3PXP"/></label><button className="primary wide" data-testid="save-item-button">{editing?'Save changes':'Encrypt & save'} <LockKeyhole size={16}/></button></form></div>}{showGen&&<div className="modal-backdrop"><div className="modal gen-modal" data-testid="generator-modal"><button type="button" className="modal-close icon-btn" data-testid="close-generator" onClick={()=>setShowGen(false)}><X/></button><p className="eyebrow">SECURITY TOOL</p><h2>Password Generator</h2><div className="gen-output" data-testid="generated-password">{genPwd||'—'}</div><div className="gen-controls"><label className="gen-option">Length: <b>{genOpts.length}</b><input type="range" min="8" max="64" value={genOpts.length} onChange={e=>{const o={...genOpts,length:+e.target.value};setGenOpts(o);setGenPwd(mkPwd(o))}}/></label><label className="gen-option"><input type="checkbox" checked={genOpts.upper} onChange={e=>{const o={...genOpts,upper:e.target.checked};setGenOpts(o);setGenPwd(mkPwd(o))}}/> A–Z Uppercase</label><label className="gen-option"><input type="checkbox" checked={genOpts.lower} onChange={e=>{const o={...genOpts,lower:e.target.checked};setGenOpts(o);setGenPwd(mkPwd(o))}}/> a–z Lowercase</label><label className="gen-option"><input type="checkbox" checked={genOpts.nums} onChange={e=>{const o={...genOpts,nums:e.target.checked};setGenOpts(o);setGenPwd(mkPwd(o))}}/> 0–9 Numbers</label><label className="gen-option"><input type="checkbox" checked={genOpts.syms} onChange={e=>{const o={...genOpts,syms:e.target.checked};setGenOpts(o);setGenPwd(mkPwd(o))}}/> !@# Symbols</label></div><div className="gen-actions"><button type="button" className="secondary" data-testid="regenerate-button" onClick={()=>setGenPwd(mkPwd(genOpts))}><RefreshCw size={15}/> Regenerate</button><button type="button" className="primary" data-testid="copy-generated-button" onClick={()=>{navigator.clipboard.writeText(genPwd).catch(()=>{const a=document.createElement('textarea');a.value=genPwd;document.body.appendChild(a);a.select();document.execCommand('copy');a.remove()});toast.success('Password copied!')}}><Copy size={15}/> Copy</button></div></div></div>}{shareModal&&<div className="modal-backdrop"><div className="modal" data-testid="share-modal"><button type="button" className="modal-close icon-btn" data-testid="close-share-modal" onClick={()=>setShareModal(null)}><X/></button><p className="eyebrow">SECURE SHARE</p><h2>Share "{shareModal.item.name}"</h2><p className="muted">This link expires in {shareModal.hours} hours. Anyone with it can view the value.</p><div className="share-link-box" data-testid="share-link-display">{shareModal.link}</div><button className="primary wide" data-testid="copy-share-link-button" onClick={()=>{navigator.clipboard.writeText(shareModal.link).catch(()=>{const a=document.createElement('textarea');a.value=shareModal.link;document.body.appendChild(a);a.select();document.execCommand('copy');a.remove()});toast.success('Link copied to clipboard!')}}><Copy size={15}/> Copy link</button></div></div>}{showSec&&secReport&&<div className="modal-backdrop"><div className="modal sec-modal" data-testid="security-report-modal"><button type="button" className="modal-close icon-btn" data-testid="close-sec-modal" onClick={()=>setShowSec(false)}><X/></button><p className="eyebrow">VAULT HEALTH</p><h2>Security Report</h2><div className="sec-score-ring" data-testid="security-score"><span className="sec-score-num" style={{color:secReport.score>=80?'var(--green)':secReport.score>=50?'#fbbf24':'var(--red)'}}>{secReport.score}</span><span className="sec-score-label">/ 100</span></div><div className="sec-stats"><div className="sec-stat"><span className="sec-stat-n" style={{color:'var(--green)'}}>{secReport.total}</span><span>Total items</span></div><div className="sec-stat"><span className="sec-stat-n" style={{color:secReport.weak.length?'var(--red)':'var(--green)'}}>{secReport.weak.length}</span><span>Weak (&lt;10 chars)</span></div><div className="sec-stat"><span className="sec-stat-n" style={{color:secReport.reused.length?'#fbbf24':'var(--green)'}}>{secReport.reused.length}</span><span>Reused</span></div><div className="sec-stat"><span className="sec-stat-n" style={{color:secReport.old.length?'#fbbf24':'var(--green)'}}>{secReport.old.length}</span><span>Old (90d+)</span></div></div>{secReport.weak.length>0&&<div className="sec-list"><p className="sec-list-title danger-text">Weak passwords</p>{secReport.weak.map((n,i)=><div key={i} className="sec-item">{n}</div>)}</div>}{secReport.reused.length>0&&<div className="sec-list"><p className="sec-list-title warn-text">Reused passwords</p>{secReport.reused.map((names,i)=><div key={i} className="sec-item">{names.join(', ')}</div>)}</div>}{secReport.old.length>0&&<div className="sec-list"><p className="sec-list-title warn-text">Not updated in 90+ days</p>{secReport.old.map((n,i)=><div key={i} className="sec-item">{n}</div>)}</div>}</div></div>}{showAudit&&<div className="modal-backdrop"><div className="modal audit-modal" data-testid="audit-modal"><button type="button" className="modal-close icon-btn" data-testid="close-audit-modal" onClick={()=>setShowAudit(false)}><X/></button><p className="eyebrow">SECURITY</p><h2>Activity Log</h2>{auditLogs.length===0?<p className="muted">No activity yet.</p>:<div className="audit-list">{auditLogs.map((e,i)=><div key={i} className="audit-row" data-testid={`audit-row-${i}`}><span className={`audit-badge ab-${e.action.toLowerCase()}`}>{e.action}</span><span className="audit-detail">{e.detail||'—'}</span><span className="audit-time">{new Date(e.ts).toLocaleString()}</span></div>)}</div>}</div></div>}<Toaster theme="dark"/></div>
}
function ShareView({token}){
  const [data,setData]=useState(null); const [err,setErr]=useState('');
  useEffect(()=>{client.get(`/share/${token}`).then(r=>setData(r.data)).catch(e=>setErr(e.response?.data?.detail||'This link has expired or is invalid'))},[token]);
  const copy=()=>{navigator.clipboard.writeText(data.value).catch(()=>{const a=document.createElement('textarea');a.value=data.value;document.body.appendChild(a);a.select();document.execCommand('copy');a.remove()});toast.success('Copied!')};
  if(err)return <div className="share-screen"><div className="brand"><img src="/toppass5-logo-sm.jpeg" alt="" className="brand-logo-mark"/></div><div className="share-card"><p className="eyebrow">SHARE LINK</p><h2>Link Expired</h2><p className="muted">{err}</p></div><Toaster theme="dark"/></div>;
  if(!data)return <LoadingScreen/>;
  return <div className="share-screen"><div className="brand"><img src="/toppass5-logo-sm.jpeg" alt="" className="brand-logo-mark"/></div><div className="share-card" data-testid="share-view-card"><p className="eyebrow">SHARED WITH YOU</p><h2>{data.name}</h2><div className="share-value-box" data-testid="share-value">{data.value}</div><button className="primary wide" onClick={copy}><Copy size={15}/> Copy value</button><p className="share-exp">Expires {new Date(data.expires).toLocaleString()}</p></div><Toaster theme="dark"/></div>
}
export default function App(){const [user,setUser]=useState(null); const [booting,setBooting]=useState(true); useEffect(()=>{const t=new URLSearchParams(window.location.search).get('token');if(t){localStorage.setItem('vault_token',t);window.history.replaceState({},'','/')} const token=localStorage.getItem('vault_token');if(token)client.get('/auth/me',authHeader()).then(r=>setUser(r.data)).catch(()=>localStorage.removeItem('vault_token')).finally(()=>setBooting(false));else setBooting(false)},[]);
  const shareToken=new URLSearchParams(window.location.search).get('share');
  if(shareToken)return <ShareView token={shareToken}/>;
  const logout=()=>{localStorage.removeItem('vault_token');setUser(null)}; if(booting)return <LoadingScreen/>; return user?<Vault user={user} onLogout={logout}/>:<Auth onLogin={setUser}/>}
function LoadingScreen(){
  return <div className="loading-screen"><div className="ls-bar"/><div className="ls-center"><div className="ls-title">TOPPASS5</div><div className="ls-sub">SECURING YOUR VAULT<span className="ls-cursor"/></div></div><div className="ls-company"><img src="https://img.sanishtech.com/u/7ad9ec964e6da7120bb20b71fd4cbcb3.png" alt="ZNQ NETWORK" className="znq-logo"/><p className="loading-by">by ZNQ NETWORK</p></div></div>
}